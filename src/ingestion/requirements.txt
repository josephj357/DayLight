# DayLight ingestion + API dependencies.
# Pinned to known-good versions. Bump deliberately, not opportunistically.

# HTTP + parsing
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# Config + types
pyyaml==6.0.2
pydantic==2.9.2

# Database
sqlite-utils==3.37

# AI synthesis
anthropic==0.39.0

# API
fastapi==0.115.4
uvicorn[standard]==0.32.0

# Test runtime (used by /tests)
pytest==8.3.3
